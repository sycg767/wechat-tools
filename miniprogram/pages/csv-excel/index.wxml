<view class="container">
  <view class="page-header">
    <view class="page-title">CSV / Excel 互转</view>
    <view class="page-desc">在 CSV 与 Excel 之间快速转换，适合日常表格整理与导出。</view>
  </view>

  <view class="card form-card mode-card">
    <view class="mode-selector">
      <view class="mode-item {{mode === 'csv-excel' ? 'active' : ''}}" bindtap="switchMode" data-mode="csv-excel">CSV 转 Excel</view>
      <view class="mode-item {{mode === 'excel-csv' ? 'active' : ''}}" bindtap="switchMode" data-mode="excel-csv">Excel 转 CSV</view>
    </view>
  </view>

  <file-picker
    file="{{selectedFile}}"
    accept="{{mode === 'csv-excel' ? 'csv' : 'excel'}}"
    title="{{mode === 'csv-excel' ? '选择 CSV 文件' : '选择 Excel 文件'}}"
    description="{{mode === 'csv-excel' ? '将 CSV 转换为 .xlsx 格式' : '将 Excel 转换为 .csv 格式'}}"
    bind:selected="handleFileSelected"
  />

  <view class="card form-card {{selectedFile ? 'form-active' : ''}}">
    <button
      class="btn btn-primary {{!selectedFile ? 'btn-disabled' : ''}}"
      bindtap="handleSubmit"
      hover-class="btn-hover"
    >
      开始转换
    </button>
  </view>

  <view class="notice notice-subtle tips-block">
    <view class="notice-title">使用提示</view>
    <view class="notice-row">1. CSV 转 Excel 默认使用 UTF-8 编码读取。</view>
    <view class="notice-row">2. Excel 转 CSV 默认导出第一个工作表内容。</view>
    <view class="notice-row">3. 转换完成后可在“任务列表”中查看历史。</view>
  </view>
</view>
